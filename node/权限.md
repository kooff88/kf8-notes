###权限

```
unix
    读1 写2   读+写3  执行4
    素数 2*3*5*7*11
```

```
  角色表             id    角色名    对该角色描述
  用户表             id    用户名    对改用户的描述
  角色-用户对应表     id    角色id    用户id 
  限制内容列表        id    名称      描述
  权限列表(增，删等)  id     名称      描述
  权限-角色-用户对应表 id    限制内容id 权限id   角色／用户类型(boolen) 角色／用户id  权限类型(boolen)
```



